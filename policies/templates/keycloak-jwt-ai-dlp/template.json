{
  "id": "keycloak-jwt-ai-dlp",
  "displayName": "Keycloak JWT — AI + DLP",
  "version": "1.1",
  "scope": "api",
  "parameters": [
    {
      "name": "ContainerAppUrl",
      "type": "string",
      "required": true,
      "description": "Base URL of the AI Policy Engine used by precheck, content-check, and log callbacks."
    }
  ]
}

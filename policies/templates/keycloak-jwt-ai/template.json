{
  "id": "keycloak-jwt-ai",
  "displayName": "Keycloak JWT — AI",
  "version": "1.1",
  "scope": "api",
  "parameters": [
    {
      "name": "ContainerAppUrl",
      "type": "string",
      "required": true,
      "description": "Base URL of the AI Policy Engine used by precheck and log callbacks."
    }
  ]
}
